# PostgreSQL Database (Vercel Postgres)
POSTGRES_URL="postgres://username:password@hostname:5432/database"
POSTGRES_PRISMA_URL="postgres://username:password@hostname:5432/database?pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://username:password@hostname:5432/database"
POSTGRES_USER="username"
POSTGRES_HOST="hostname"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="database"

# JWT Secret for Authentication (generate a strong random string)
JWT_SECRET="your-very-secure-jwt-secret-key-change-this"

# API Base URL (for frontend to connect to backend)
# For local development with vercel dev
VITE_API_URL=http://localhost:3000/api
# For production, use: /api